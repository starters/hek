    USAGE

        hek [commands] [options]

    COMMANDS

      start                      Start a new project. (Default command)
      install    [modules...]    Install a starter module. e.g hek install redux-starter
      link       [name]          Link the starter module in current working directory.
      unlink     [name]          Unlink given starter module.
      list                       List all installed & linked starter modules in the cache.

    OPTIONS

        -C     --no-cache      Disable module caching.
        -v     --version       Show version and exit
        -h     --help          Show help and exit
